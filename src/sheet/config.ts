export interface SheetOptions {
  scale: number
  color: string | number
  edgeColor: string | number
}

export const SHEET_OPTIONS: SheetOptions = {
  scale: 2,
  color: 0x00ff00,
  edgeColor: 0xff0000
}
