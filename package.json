{
  "type": "module",
  "dependencies": {
    "gsap": "^3.7.0",
    "rxjs": "^7.1.0",
    "three": "^0.129.0"
  },
  "devDependencies": {
    "browser-sync": "^2.27.3",
    "dat.gui": "^0.7.7",
    "esbuild": "^0.12.9",
    "rxjs-debug": "^1.3.0"
  },
  "browser": {
    "path": "build"
  },
  "scripts": {
    "build": "esbuild sheet.js --bundle --minify --sourcemap --outfile=build/app.js",
    "dev": "esbuild sheet.js --watch --bundle --sourcemap --outfile=build/app.js & browser-sync start --server --files 'build' 'sheetview.html'"
  }
}
