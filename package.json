{
  "type": "module",
  "dependencies": {
    "dat.gui": "^0.7.7",
    "gsap": "^3.7.0",
    "rxjs": "^7.1.0",
    "three": "^0.129.0"
  },
  "devDependencies": {
    "esbuild": "^0.12.9",
    "http-server": "^0.12.3"
  },
  "browser": {
    "path": "build"
  },
  "scripts": {
    "build": "esbuild sheet.js --bundle --minify --sourcemap --outfile=build/app.js"
  }
}
