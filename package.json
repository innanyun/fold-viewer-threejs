{
  "dependencies": {
    "gsap": "^3.7.0",
    "rxjs": "^7.1.0",
    "three": "^0.129.0"
  },
  "devDependencies": {
    "browser-sync": "^2.27.3",
    "dat.gui": "^0.7.7",
    "esbuild": "^0.12.9",
    "eslint": "^7.30.0",
    "jshint": "^2.13.0",
    "rxjs-debug": "^1.3.0"
  },
  "browser": {
    "path": "build"
  },
  "scripts": {
    "build": "esbuild src/view.js --bundle --format=esm --minify --outdir=build",
    "dev": "esbuild src/view.js --watch --bundle --format=esm --sourcemap --outdir=build & browser-sync start --server --files 'build' '*.html'"
  }
}
